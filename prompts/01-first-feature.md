This project is brand new — it doesn't do anything yet. Add a command-line entry point
so `skinner` can be run from the terminal, starting with a `--version` flag that prints
the package version. Include a test. When it works, squash the work onto `main` as a
single commit.

No one is available to answer questions: make the choices the work needs, and note what
you assumed.
