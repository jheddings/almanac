The README lists a planned feature: normalizing a name to a URL-safe ASCII slug. Build
it — a `--slug NAME` flag that prints the slug and exits. Include a test. When it works,
squash the work onto `main` as a single commit.
