The README lists a planned feature: normalizing a name to a URL-safe ASCII slug. Build
it — a `--slug NAME` flag that prints the slug and exits. Include a test. Commit the
work on its own branch when it's done.
