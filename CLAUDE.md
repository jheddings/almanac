# CLAUDE.md

Instructions for this repo live in [AGENTS.md](AGENTS.md) — read it. In particular, it
covers [`docs/almanac/`](docs/almanac/), this repository's operating knowledge — the
facts it learned the hard way and the rules it holds you to. Run `ls docs/almanac/` at
the start of a session; grep it before investigating unexpected behavior.
